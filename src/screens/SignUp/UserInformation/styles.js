import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  formCadastro: {
    display: "flex",
    height: "100%",
    width: "65%",
    alignItems: "center",
  },
});

export default styles;
